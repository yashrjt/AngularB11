import { CalculateBonusPipe } from './calculate-bonus.pipe';

describe('CalculateBonusPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateBonusPipe();
    expect(pipe).toBeTruthy();
  });
});
